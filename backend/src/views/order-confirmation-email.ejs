<!-- Contenido para: backend/src/views/order-confirmation-email.ejs -->
<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Confirmación de Pedido - Sofilu</title>
    <style>
      body {
        font-family: sans-serif;
        margin: 0;
        padding: 20px;
        background-color: #faf3e9;
        color: #36454f;
      }
      .container {
        max-width: 600px;
        margin: auto;
        background-color: white;
        padding: 20px;
        border-radius: 16px;
      }
      h1 {
        font-family: "Times New Roman", serif;
      }
      table {
        width: 100%;
        border-collapse: collapse;
      }
      th,
      td {
        padding: 10px;
        border-bottom: 1px solid #eae3d9;
        text-align: left;
      }
      .total-row {
        font-weight: bold;
        font-size: 1.2em;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1 style="text-align: center">
        ¡Gracias por tu pedido, <%= order.customerInfo.name %>!
      </h1>
      <p style="text-align: center">
        Hemos recibido tu pedido y lo estamos procesando. Este es un resumen de
        tu compra:
      </p>

      <hr />
      <h3>Productos</h3>
      <table>
        <tbody>
          <% order.items.forEach(item => { %>
          <tr>
            <td>
              <img
                src="<%= item.product.images[0] %>"
                alt="<%= item.product.name %>"
                width="60"
                style="border-radius: 8px"
              />
            </td>
            <td>
              <strong><%= item.product.name %></strong><br />
              <small>
                <% for (const [key, value] of
                Object.entries(item.selectedVariants)) { %> <%= key %>: <%=
                value %> <% } %>
              </small>
            </td>
            <td>x <%= item.quantity %></td>
            <td style="text-align: right">
              $<%= item.price.toLocaleString('es-CO') %>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>

      <hr />
      <h3>Resumen</h3>
      <table>
        <tbody>
          <tr>
            <td>Subtotal</td>
            <td style="text-align: right">
              $<%= (order.totalAmount - 10000).toLocaleString('es-CO') %>
            </td>
          </tr>
          <tr>
            <td>Envío</td>
            <td style="text-align: right">$10,000</td>
          </tr>
          <tr class="total-row">
            <td>Total</td>
            <td style="text-align: right">
              $<%= order.totalAmount.toLocaleString('es-CO') %>
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  </body>
</html>
