import { DynamicTheme } from './dynamic-theme';

describe('DynamicTheme', () => {
  it('should create an instance', () => {
    const directive = new DynamicTheme();
    expect(directive).toBeTruthy();
  });
});
