import { ImageLoader } from './image-loader';

describe('ImageLoader', () => {
  it('should create an instance', () => {
    const directive = new ImageLoader();
    expect(directive).toBeTruthy();
  });
});
