import { NumericFormat } from './numeric-format';

describe('NumericFormat', () => {
  it('should create an instance', () => {
    const directive = new NumericFormat();
    expect(directive).toBeTruthy();
  });
});
